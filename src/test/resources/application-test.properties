# Disable Stripe during tests
stripe.enabled=false

# Test configuration for Stripe (disabled)
stripe.public.key=pk_test_disabled_for_tests
stripe.secret.key=sk_test_disabled_for_tests
stripe.webhook.secret=whsec_disabled_for_tests

# Application configuration
app.base.url=http://localhost:8080
ssl.enabled=false
